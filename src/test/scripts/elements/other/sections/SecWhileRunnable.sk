test "SkBee - Other/SecWhileRunnable":
	parse:
		while 1 = 2 repeating every 5 seconds:
			set {_a} to {_a}

	if parse logs is set:
		# This for some reason happens locally
		assert parse logs = "The condition '1 is equal to 2' will always be false."
	else:
		# This one happens in GitHub Actions
		assert parse logs is not set with "Shouldn't get an error if this parses correctly"
